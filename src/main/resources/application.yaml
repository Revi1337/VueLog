spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/vuelog
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true


logging:
  level:
    sql: debug
